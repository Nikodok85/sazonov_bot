from flask import Flask, request
import telebot

TOKEN = "6386592043:AAGkr2SqqoXezvTZXXjDiUeLZJELg-BLD8A"
CHANNEL_ID = "@gynekolog_Sazonov"

bot = telebot.TeleBot(TOKEN)
app = Flask(__name__)

# Команда /start — приветствие
@bot.message_handler(commands=['start'])
def send_welcome(message):
    markup = telebot.types.ReplyKeyboardMarkup(resize_keyboard=True)
    markup.add(telebot.types.KeyboardButton("🟢 Задать вопрос"))
    welcome_text = (
        "Привет! 🧬\n\n"
        "Ты можешь анонимно задать любой вопрос по гинекологии.\n"
        "Нажми кнопку ниже, чтобы написать свой вопрос:"
    )
    bot.send_message(message.chat.id, welcome_text, reply_markup=markup)

# Нажатие кнопки "Задать вопрос"
@bot.message_handler(func=lambda message: message.text == "🟢 Задать вопрос")
def handle_question_prompt(message):
    bot.send_message(message.chat.id, "✍️ Напиши свой вопрос: я получу его анонимно и отвечу в канале.")

# Получение текста и пересылка в канал
@bot.message_handler(func=lambda message: True, content_types=['text'])
def forward_to_channel(message):
    if message.text != "🟢 Задать вопрос":
        bot.send_message(CHANNEL_ID, f"❓ Анонимный вопрос:\n\n{message.text}")
        bot.send_message(message.chat.id, "✅ Вопрос получен! Я обязательно отвечу в этом канале.")

# Webhook для Telegram
@app.route("/" + TOKEN, methods=["POST"])
def webhook():
    json_str = request.get_data().decode("utf-8")
    update = telebot.types.Update.de_json(json_str)
    bot.process_new_updates([update])
    return "OK", 200

# Health check
@app.route("/", methods=["GET"])
def root():
    return "Bot is working", 200
